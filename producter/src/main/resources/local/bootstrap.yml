spring:
  application:
    name: producter
  cloud:
    consul:
      discovery:
        host: localhost
        port: 8500
        register: true
        enabled: true
        healthCheckPath: /health/status
        healthCheckInterval: 10s
      config:
        failFast: false
        enabled: true
        prefix: config
        defaultContext: apps
        profileSeparator: '::'

#feign:
#  hystrix:
#    enabled: true
#
#hystrix:
#  command:
#    "IRankerService#rank(RankRequest)":
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 100
#      circuitBreaker:
#        errorThresholdPercentage: 60
#        sleepWindowInMilliseconds: 2000
#      metrics:
#        rollingStats:
#          timeInMilliseconds: 5000
#          numBuckets: 50
#    "IRecallerService#recall(RecallRequest)":
#        execution:
#          isolation:
#            thread:
#              timeoutInMilliseconds: 100
#        circuitBreaker:
#          errorThresholdPercentage: 60
#          sleepWindowInMilliseconds: 2000
#        metrics:
#          rollingStats:
#            timeInMilliseconds: 5000
#            numBuckets: 50
#
#
management:
  security:
    enabled: false
  health:
    consul:
      enabled: false